/**
 * COMPARTIDO: DTOs HTTP (re-exported para compatibilidad)
 * 
 * Nota: Los DTOs están en src/http/dtos/ porque pertenecen a la capa HTTP.
 * Este archivo solo re-exporta para mantener compatibilidad con código que
 * aún importa desde @/shared/dtos
 */

export * from "@/http/dtos"
